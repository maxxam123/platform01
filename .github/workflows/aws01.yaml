on:
 push:
   branches:
   - main
   paths:
   - 'cluster/aws01/cluster.yaml'


env:
 AWS_ACCESS_KEY_ID: ${{ SECRET_KEY_01rets.ACCESS_KEY_01 }}
 AWS_SECRET_ACCESS_KEY: ${{ SECRET_KEY_01rets.SECRET_KEY_01 }}
 BUCKET_TF_STATE: ${{ SECRET_KEY_01rets.BUCKET_TF_STATE_01 }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: test
        run: |
          ls -la
