on:
 push:
   branches:
   - main
   paths:
   - 'cluster/aws02/cluster.yaml'


env:
 AWS_ACCESS_KEY_ID: ${{ SECRET_KEY_02rets.ACCESS_KEY_02 }}
 AWS_SECRET_ACCESS_KEY: ${{ SECRET_KEY_02rets.SECRET_KEY_02 }}
 BUCKET_TF_STATE: ${{ SECRET_KEY_02rets.BUCKET_TF_STATE_02 }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: test
        run: |
          ls -la
