on:
 push:
   branches:
   - main
   paths:
   - 'cluster/aws03/cluster.yaml'


env:
 AWS_ACCESS_KEY_ID: ${{ SECRET_KEY_03rets.ACCESS_KEY_03 }}
 AWS_SECRET_ACCESS_KEY: ${{ SECRET_KEY_03rets.SECRET_KEY_03 }}
 BUCKET_TF_STATE: ${{ SECRET_KEY_03rets.BUCKET_TF_STATE_03 }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: test
        run: |
          ls -la
